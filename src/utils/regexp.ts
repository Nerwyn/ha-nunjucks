// TODO implement
export function match(
	value: string,
	find: RegExp,
	ignorecase: boolean = false,
) {}

export function search(
	value: string,
	find: RegExp,
	ignorecase: boolean = false,
) {}

export function regex_replace(
	value: string,
	find: RegExp | string = '',
	replace: RegExp | string = '',
	ignorecase: boolean = false,
) {}

export function regex_findall(
	value: string,
	find: RegExp | string = '',
	replace: RegExp | string = '',
	ignorecase: boolean = false,
) {}

export function regex_findall_index(
	value: string,
	find: RegExp | string = '',
	index: number = 0,
	ignorecase: boolean = false,
) {}
